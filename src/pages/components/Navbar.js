const navBarStyle = {
    backgroundColor: "#eaeaea",
    color: "black",
    height: "50px",
    borderRadius: '8px',
    marginTop: '10px',
  };
  
  const NavBar = () => (
    <div className="NavBar" style={navBarStyle}>
      NAVBAR
    </div>
  );
  
  export default NavBar;